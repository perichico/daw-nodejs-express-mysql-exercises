<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos de la Marca - <%= marca.nombreMarca %></title>
</head>
<body>
    <h1>Productos de la Marca: <%= marca.nombreMarca %></h1>

    <a href="/marcas">Volver a la lista de marcas</a>

    <% if (productos.length > 0) { %>
        <table border="1">
            <thead>
                <tr>
                    <th>Nombre del Producto</th>
                    <th>Precio</th>
                    <th>Categoría</th>
                </tr>
            </thead>
            <tbody>
                <% productos.forEach(producto => { %>
                    <tr>
                        <td><%= producto.nombreProducto %></td>
                        <td><%= producto.precio %>€</td>
                        <td><%= producto.categoria %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    <% } else { %>
        <p>No hay productos para esta marca.</p>
    <% } %>
</body>
</html>
